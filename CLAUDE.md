# CLAUDE.md - masker プロジェクト指示書

最終更新: 2026-04-02  
フェーズ: MVP (v0.1) 実装前

---

## 1. このプロジェクトについて

テキストデータ内の個人情報・機密情報をパターン認識で検知し、自動的にマスキング処理を行うCLIツール。

- **仕様書**: `docs/spec.md` を必ず参照すること
- **プロダクトID**: 003
- **仮称**: masker

---

## 2. 実装方針

- 言語: **Python**（MVP）
- 配布形式: PyInstallerで生成した単一exeファイル
- 対象OS: Windows 11（MVP）
- 外部APIへの通信は**一切行わない**（完全ローカル処理を厳守）
- 設計書は作成しない。実装上の判断は本ファイルの「decisions」セクションに随時記録する

---

## 3. ディレクトリ構成

```
003_masker/
├── docs/
│   └── spec.md          # 仕様書（必読）
├── src/
│   └── masker.py        # 実装本体
├── tests/
│   └── test_masker.py   # テストコード
├── testdata/
│   └── sample.txt       # テスト用入力ファイル
├── README.md            # ユーザー向け説明書
└── CLAUDE.md            # 本ファイル
```

---

## 4. 禁止事項

- 外部APIへの通信を行うコードを書かないこと
- プロジェクトフォルダパスに日本語・全角文字を含めないこと
- インストーラーを必要とする配布形式にしないこと
- v0.2以降の機能（フォルダ一括処理・Shift-JIS対応・P07〜P10パターン等）をMVPに混入しないこと

---

## 5. MVP実装スコープ（v0.1）

以下のみを実装する。それ以外はv0.2以降。

### 対象パターン
| ID | 種別 |
|----|------|
| P01 | メールアドレス |
| P02 | 電話番号（固定・携帯） |
| P03 | 郵便番号 |
| P04 | クレジットカード番号 |
| P05 | マイナンバー |
| P06 | IPアドレス（IPv4） |

### 対応入力
- ファイル指定（引数）
- 標準入力（stdin・パイプ）
- 文字コード: UTF-8のみ
- 対応拡張子: .txt / .log / .csv / .md

### 対応出力
- stdout（デフォルト）
- ファイル出力（`-o` オプション）
- サマリーレポート（stderr）

### CLIオプション
`--output/-o` `--mask/-m` `--only` `--exclude` `--quiet/-q` `--version/-v` `--help/-h`

詳細は `docs/spec.md` のセクション6を参照。

---

## 6. 実装上の決定事項（decisions）

※ 実装を進める中で判断した内容をここに随時追記する。

| 日付 | 判断内容 | 理由 |
|------|----------|------|
| 2026-04-02 | PythonでMVPを実装し、Rustリライトは完成後に別途判断する | ウォーミングアップとして「作りきること」を最優先とするため |
| 2026-04-02 | 設計書は作成しない | 仕様書で実装に必要な情報が揃っており、このスケールでは不要と判断 |
| 2026-04-02 | exe化にはPyInstallerを使用する | 実績・情報量が最も多く、CLIツールとの相性が良いため |
| 2026-04-02 | アンチウイルス誤検知対策としてGitHubでソースを公開する | コード署名証明書はMVP段階では費用対効果が低いため |

---

## 7. 現在の進捗

| フェーズ | ステータス |
|----------|------------|
| 仕様策定 | ✅ 完了（docs/spec.md） |
| 設計 | ✅ 省略（仕様書で代替） |
| 実装（Python MVP） | 🔲 未着手 |
| テスト | 🔲 未着手 |
| PyInstaller exe化 | 🔲 未着手 |
| GitHub公開・README作成 | 🔲 未着手 |
| Rustリライト検討 | 🔲 完成後に判断 |

---

## 8. 次回セッションへの引き継ぎ事項

※ セッション終了前に「次回セッションへの引き継ぎ事項をCLAUDE.mdに反映して」と依頼することで随時更新する。

- 現時点では実装未着手。次のセッションでは `src/masker.py` の実装から開始する
- 最初に取り組むべき実装: P01（メールアドレス）とP02（電話番号）の正規表現とCLI基本構造

---

## 9. 開発者情報（参考）

- 使用可能言語: Python / C・C++ / Java / Rust（学習中） 他
- 作業時間: 1日1〜3時間・週10〜15時間・月40〜60時間程度
- 開発環境: Windows 11 / ClaudeCode
- 目標: 収益化・開発全工程経験・Rust習得（優先順位順）
